# Production Environment Configuration
NODE_ENV=production
PORT=3001

# Kafka Configuration (Production - Docker service name)
KAFKA_BROKER=kafka:29092

# gRPC Configuration (Production - Docker service names)
# Service1's own gRPC server port
GRPC_HOST=0.0.0.0
GRPC_PORT=5000

# Service2's gRPC server (for client connection)
SERVICE2_GRPC_HOST=service2
SERVICE2_GRPC_PORT=5001

# Database Configuration (if needed later)
# DATABASE_URL=postgresql://kong-database:5432/userdb

# Logging
LOG_LEVEL=info