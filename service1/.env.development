# Development Environment Configuration
NODE_ENV=development
PORT=3001

# Kafka Configuration (Development - localhost)
KAFKA_BROKER=localhost:9092

# gRPC Configuration (Development - localhost)
# Service1's own gRPC server port
GRPC_HOST=localhost
GRPC_PORT=5000

# Service2's gRPC server (for client connection)
SERVICE2_GRPC_HOST=localhost
SERVICE2_GRPC_PORT=5001

# Database Configuration (if needed later)
# DATABASE_URL=postgresql://localhost:5432/userdb

# Logging
LOG_LEVEL=debug